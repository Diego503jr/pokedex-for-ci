name: Say Hello

on:
  push:
    branches:
      - main

jobs:
  Hello_World:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Message
        run: |
          echo "Hello, World!"
      - name: Dime la fecha
        run: ls -l
